# Research Datasets

This directory is reserved for hosting datasets generated during runtime experiments (e.g. system call event logs, resource metrics, benchmarks).

Currently no active external dataset is required.