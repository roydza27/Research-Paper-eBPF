# Experimental Environment

This directory records the host configuration required to interpret Phase 3 measurements.

Run `./collect-environment.sh` from this directory on the experimental host. The script emits a timestamped JSON manifest and a human-readable summary without assuming a particular Linux distribution.

The manifest must be preserved with every benchmark result.